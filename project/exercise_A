#include <stdio.h>
#include <string.h>
 

int main()
{
char seq[1000];
int i, length;
float gc = 0;

scanf("%s", &seq);
while (seq != 'ATG') {
scanf("%s", &seq);
}
while (seq != 'TAA' || seq != 'TAG' || seq != 'TGA') {
printf(("%f\n", &seq));
scanf ("%s",seq);
}
printf(("%s", &seq));
return 0;
}
